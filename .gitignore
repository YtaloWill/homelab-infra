# OpenTofu
**/.terraform/
*.tfstate
*.tfstate.*
*.tfvars
!*.tfvars.example
crash.log

# Ansible secrets (only the .example ships)
ansible/inventory/group_vars/all/vault.yml
.vault_pass
*.retry

# Misc
.env

# Claude Code reads CLAUDE.md; AGENTS.md is the tracked source of truth.
# Create a local symlink instead of committing a duplicate (see README).
CLAUDE.md
